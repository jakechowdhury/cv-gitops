apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
images:
  - name: ghcr.io/jakechowdhury/cv-site
    newTag: 0.1.8  # bumped to git SHA by cv-site CI pipeline via PR