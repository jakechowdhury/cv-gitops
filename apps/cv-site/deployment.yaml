apiVersion: apps/v1
kind: Deployment
metadata:
  name: cv-site
  namespace: cv-site
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cv-site
  template:
    metadata:
      labels:
        app: cv-site
    spec:
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: cv-site
          image: ghcr.io/jakechowdhury/cv-site
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 100m
              memory: 128Mi
          livenessProbe:
            httpGet:
              path: /
              port: 8080
          readinessProbe:
            httpGet:
              path: /
              port: 8080
